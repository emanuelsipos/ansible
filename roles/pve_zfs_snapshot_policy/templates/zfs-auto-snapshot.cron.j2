# Managed by Ansible role pve_zfs_snapshot_policy.
SHELL=/bin/sh
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

{% if _pve_zfs_snapshot_policy_creation_enabled %}
{% for tier in pve_zfs_snapshot_policy_tiers | dict2items %}
{{ tier.value.schedule }} root {{ pve_zfs_snapshot_policy_executable }} --quiet --syslog --skip-scrub --default-exclude --prefix={{ pve_zfs_snapshot_policy_prefix }} --label={{ tier.key }} //
{% endfor %}
{% else %}
# Report-only mode: this host is inactive.
{% endif %}
