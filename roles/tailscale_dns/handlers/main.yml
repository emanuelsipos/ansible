---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart systemd-resolved
  ansible.builtin.systemd_service:
    name: "{{ tailscale_dns_resolved_service }}"
    state: restarted
